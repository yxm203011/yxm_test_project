Mysql的四个隔离级别
1.读取未提交数据(read uncommitted)
比如抢票系统：
A用户预定了a座位但是没有没有执行commit操作 数据还在缓存中  但是B用户过来预定了a座位那肯定是不行的所以就需要用到可查询缓存中的数据了
2.读取已提交数据(read committed)
比如转账操作：
转账操作就必须获取已经提交的数据 如果A用户支出但支出是错误的 钱是要退回的  然后  银行又要像A转账  那就必须要获取 提交的数据 不能获取缓存
3.重复读取(repeatable read)
比如商城操作：
用户将一个商品加入购物车要付款的时候  商家将这个商品涨价了  那我用户肯定是需要 按涨价之前的价格购买商品
4.序列化(serializable)
设置了这个事务  那么所有的事物都必须等待上个事务执行完才会执行当前这个事务
